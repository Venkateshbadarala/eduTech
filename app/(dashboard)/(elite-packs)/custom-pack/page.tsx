import CustomPackSection from '@/Components/landingPage/CustomPackSection'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
        <CustomPackSection/>
    </div>
  )
}

export default page